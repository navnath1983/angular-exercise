import { BorderChangeDirective } from './border-change.directive';

describe('BorderChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new BorderChangeDirective();
    expect(directive).toBeTruthy();
  });
});
